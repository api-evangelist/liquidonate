# authorship: generated by API Evangelist tooling. Stamped 2026-08-18
# on the file's own generator header (roadmap#64). An unmarked file is
# NOT assumed to be ours -- absence of evidence was never stamped.
method: generated
overlay: 1.0.0
info:
  title: API Evangelist enhancements for MagicMatch by LiquiDonate
  version: 1.0.0
extends: openapi/liquidonate-magicmatch-openapi.yml
actions:
- target: $.info
  update:
    x-apievangelist-source: https://docs-magicmatch.liquidonate.com
    x-apievangelist-reconstructed: true
    x-apievangelist-note: LiquiDonate publishes no machine-readable API definition. This OpenAPI was reconstructed
      by the API Evangelist enrichment pipeline from the published Postman collection, then verified against
      the live host.
    x-apievangelist-generated: '2026-07-19'
- target: $.servers[0]
  update:
    x-verified: true
    x-verified-method: unauthenticated POST returns 401 {"error":"invalid api key or secret"}
    x-verified-date: '2026-07-19'
- target: $.paths['/v1/matchAndShip'].post
  update:
    x-recommended-precondition: Call estimateMatchAndShip first and pass the returned match_uuid so the
      previewed nonprofit match and shipping rate are preserved.
    x-excluded-categories:
    - furniture
    - mattress
    - drug paraphernalia
    x-weight-limit-kg: 50
    x-excluded-return-reasons:
    - damaged
- target: $.paths['/v1/match'].post
  update:
    x-required-followup: If you purchase your own label, call attachLabelsToMatch so the nonprofit is
      notified and donation receipts are generated.
- target: $.paths['/v1/ship'].post
  update:
    x-no-donation-receipt: true
    x-note: No nonprofit matching is performed and no donation receipt is generated by this operation.
- target: $.components.schemas.CategoryType
  update:
    x-vocabulary: vocabulary/liquidonate-category-types.yml
